import Login from '@/Pages/Login/Login';
import React from 'react';
export const metadata = {
  title: "Login",
  description: "poultry hub login page.",
};
const LogIn = () => {
  return (
    <div>
      <Login></Login>
    </div>
  );
};

export default LogIn;