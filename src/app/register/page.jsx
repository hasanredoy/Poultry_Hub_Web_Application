import Login from '@/Pages/Login/Login';
import Register from '@/Pages/Register/Register';
import React from 'react';
export const metadata = {
  title: "Register",
  description: "poultry hub register page, ",
};
const RegisterPage = () => {
  return (
    <div>
      <Register></Register>
    </div>
  );
};

export default RegisterPage;